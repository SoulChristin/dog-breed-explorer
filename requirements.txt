requests==2.34.2
dbt-core==1.8.8
dbt-duckdb==1.8.3

# Dashboard. duckdb and pandas are pinned to the versions dbt-duckdb already
# resolves to, so installing the dashboard never downgrades the pipeline.
streamlit==1.59.2
duckdb==1.5.4
pandas==3.0.1
